<% image_path = case size
                when :thumbnail
                  person.thumbnail_image.presence
                when :full
                  person.full_image.presence
                end %>

<% if image_path.present? %>
  <%= image_tag image_path,
      alt:   person.full_name,
      class: "person-image #{size}" %>
<% end %>